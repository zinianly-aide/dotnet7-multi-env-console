name: dotnet-ci

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

concurrency:
  group: dotnet-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '7.0.x'
          cache: true
          cache-dependency-path: |
            **/*.csproj
            **/*.fsproj
            **/packages.lock.json
            **/nuget.config
        env:
          DOTNET_NOLOGO: true

      - name: Restore
        run: dotnet restore MultiModuleApp.sln

      - name: Build
        run: dotnet build MultiModuleApp.sln --configuration Release --no-restore

      - name: Test (skip if no test projects)
        shell: bash
        run: |
          set -e
          # If there are any *Test*.csproj or *.Tests.csproj, run tests; otherwise skip.
          if find . -type f \( -name "*Tests*.csproj" -o -name "*Test*.csproj" \) | grep -q .; then
            dotnet test MultiModuleApp.sln --configuration Release --no-build
          else
            echo "No test projects found, skipping dotnet test."
          fi
